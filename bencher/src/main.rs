#[rustfmt::skip]
pub fn bench_2022(output: &mut Vec<utils::BenchResult>, max_time: Option<std::time::Duration>) {
    {   pub use solutions::p2022_01::*;
        output.push(utils::bench(part_1, 1, DATE, max_time));
        output.push(utils::bench(part_2, 2, DATE, max_time));
    }

    {   pub use solutions::p2022_02::*;
        output.push(utils::bench(part_1, 1, DATE, max_time));
        output.push(utils::bench(part_2, 2, DATE, max_time));
    }

    {   pub use solutions::p2022_03::*;
        output.push(utils::bench(part_1, 1, DATE, max_time));
        output.push(utils::bench(part_2, 2, DATE, max_time));
    }

    {   pub use solutions::p2022_03::*;
        output.push(utils::bench(part_1, 1, DATE, max_time));
        output.push(utils::bench(part_2, 2, DATE, max_time));
    }

    {   pub use solutions::p2022_04::*;
        output.push(utils::bench(part_1, 1, DATE, max_time));
        output.push(utils::bench(part_2, 2, DATE, max_time));
    }

    {   pub use solutions::p2022_05::*;
        output.push(utils::bench(part_1, 1, DATE, max_time));
        output.push(utils::bench(part_2, 2, DATE, max_time));
    }

    {   pub use solutions::p2022_06::*;
        output.push(utils::bench(part_1, 1, DATE, max_time));
        output.push(utils::bench(part_2, 2, DATE, max_time));
    }

    {   pub use solutions::p2022_07::*;
        output.push(utils::bench(part_1, 1, DATE, max_time));
        output.push(utils::bench(part_2, 2, DATE, max_time));
    }

    {   pub use solutions::p2022_08::*;
        output.push(utils::bench(part_1, 1, DATE, max_time));
        output.push(utils::bench(part_2, 2, DATE, max_time));
    }

    {   pub use solutions::p2022_09::*;
        output.push(utils::bench(part_1, 1, DATE, max_time));
        output.push(utils::bench(part_2, 2, DATE, max_time));
    }

    {   pub use solutions::p2022_10::*;
        output.push(utils::bench(part_1, 1, DATE, max_time));
        output.push(utils::bench(part_2, 2, DATE, max_time));
    }

    {   pub use solutions::p2022_11::*;
        output.push(utils::bench(part_1, 1, DATE, max_time));
        output.push(utils::bench(part_2, 2, DATE, max_time));
    }

    {   pub use solutions::p2022_12::*;
        output.push(utils::bench(part_1, 1, DATE, max_time));
        output.push(utils::bench(part_2, 2, DATE, max_time));
    }

    {   pub use solutions::p2022_13::*;
        output.push(utils::bench(part_1, 1, DATE, max_time));
        output.push(utils::bench(part_2, 2, DATE, max_time));
    }

    {   pub use solutions::p2022_14::*;
        output.push(utils::bench(part_1, 1, DATE, max_time));
        output.push(utils::bench(part_2, 2, DATE, max_time));
    }

    {   pub use solutions::p2022_15::*;
        output.push(utils::bench(part_1, 1, DATE, max_time));
        // output.push(utils::bench(part_2, 2, DATE, max_time));
    }

    {   pub use solutions::p2022_16::*;
        // output.push(utils::bench(part_1, 1, DATE, max_time));
        // output.push(utils::bench(part_2, 2, DATE, max_time));
    }

    {   pub use solutions::p2022_17::*;
        output.push(utils::bench(part_1, 1, DATE, max_time));
        // output.push(utils::bench(part_2, 2, DATE, max_time));
    }

    {   pub use solutions::p2022_18::*;
        output.push(utils::bench(part_1, 1, DATE, max_time));
        output.push(utils::bench(part_2, 2, DATE, max_time));
    }

    {   pub use solutions::p2022_19::*;
        output.push(utils::bench(part_1, 1, DATE, max_time));
        output.push(utils::bench(part_2, 2, DATE, max_time));
    }

    {   pub use solutions::p2022_20::*;
        output.push(utils::bench(part_1, 1, DATE, max_time));
        output.push(utils::bench(part_2, 2, DATE, max_time));
    }
}

#[rustfmt::skip]
pub fn bench_2023(output: &mut Vec<utils::BenchResult>, max_time: Option<std::time::Duration>) {
    {   pub use solutions::p2023_01::*;
        output.push(utils::bench(part_1, 1, DATE, max_time));
        output.push(utils::bench(part_2, 2, DATE, max_time));
    }

    {   pub use solutions::p2023_02::*;
        output.push(utils::bench(part_1, 1, DATE, max_time));
        output.push(utils::bench(part_2, 2, DATE, max_time));
    }

    {   pub use solutions::p2023_03::*;
        output.push(utils::bench(part_1, 1, DATE, max_time));
        output.push(utils::bench(part_2, 2, DATE, max_time));
    }

    {   pub use solutions::p2023_04::*;
        output.push(utils::bench(part_1, 1, DATE, max_time));
        output.push(utils::bench(part_2, 2, DATE, max_time));
    }

    {   pub use solutions::p2023_05::*;
        output.push(utils::bench(part_1, 1, DATE, max_time));
        // output.push(utils::bench(part_2, 2, DATE, max_time));
    }

    {   pub use solutions::p2023_06::*;
        output.push(utils::bench(part_1, 1, DATE, max_time));
        output.push(utils::bench(part_2, 2, DATE, max_time));
    }

    // {   pub use solutions::p2023_07::*;
    //     output.push(utils::bench(part_1, 1, DATE, max_time));
    //     output.push(utils::bench(part_2, 2, DATE, max_time));
    // }

    {   pub use solutions::p2023_08::*;
        output.push(utils::bench(part_1, 1, DATE, max_time));
        output.push(utils::bench(part_2, 2, DATE, max_time));
    }

    {   pub use solutions::p2023_09::*;
        output.push(utils::bench(part_1, 1, DATE, max_time));
        output.push(utils::bench(part_2, 2, DATE, max_time));
    }

    {   pub use solutions::p2023_10::*;
        output.push(utils::bench(part_1, 1, DATE, max_time));
        output.push(utils::bench(part_2, 2, DATE, max_time));
    }

    {   pub use solutions::p2023_11::*;
        output.push(utils::bench(part_1, 1, DATE, max_time));
        output.push(utils::bench(part_2, 2, DATE, max_time));
    }
}

#[rustfmt::skip]
fn main() {
    let max_time = Some(std::time::Duration::from_secs(10));

    let mut results = vec![];

    // bench_2022(&mut results, max_time);
    bench_2023(&mut results, max_time);

    println!("\n\n== Benchmarks by Date and Part ==");
    utils::summarize_results(&results);

    results.sort_by(|a, b| a.average.partial_cmp(&b.average).unwrap());

    println!("\n== Benchmarks by Speed ==");
    utils::summarize_results(&results);
    println!("");
}
